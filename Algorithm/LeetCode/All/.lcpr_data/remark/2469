{"data":[{"name":"2025-02-24 15:54:51","id":1740383691545,"body":"# 2389. 和有限的最长子序列\r\n\r\n## 一、题目分析\r\n\r\n### 1. 题目要求\r\n给定一个整数数组 `nums` 和查询数组 `queries`，返回结果数组 `answer`，其中 `answer[i]` 是 `nums` 中和不超过 `queries[i]` 的最长子序列长度。\r\n\r\n**输入输出格式**：\r\n- 输入：`nums = [4,5,2,1], queries = [3,10,21]`\r\n- 输出：`[2,3,4]`\r\n\r\n**约束条件**：\r\n- `n == nums.length`\r\n- `m == queries.length`\r\n- `1 <= n, m <= 10^5`\r\n- `1 <= nums[i], queries[i] <= 10^9`\r\n\r\n### 2. 关键概念\r\n- **子序列**：不要求连续但保持相对顺序（通过排序可优化）\r\n- **前缀和**：前k个元素的和具有单调性\r\n- **二分查找**：在有序数组中快速定位边界\r\n\r\n## 二、示例分析\r\n\r\n### 1. 基本示例\r\n```\r\n输入：nums = [4,5,2,1], queries = [3,10,21]\r\n输出：[2,3,4]\r\n解释：\r\n1. 排序后数组变为[1,2,4,5]\r\n2. 计算前缀和[1,3,7,12]\r\n3. 查询3 → 找到最后一个≤3的前缀和是3（索引1）→ 长度2\r\n4. 查询10 → 找到最后一个≤10的前缀和是7（索引2）→ 长度3\r\n5. 查询21 → 所有元素和12≤21 → 长度4\r\n```\r\n\r\n### 2. 边界情况\r\n```\r\n输入：nums = [5], queries = [1,5,10]\r\n输出：[0,1,1]\r\n解释：\r\n- 查询1：5>1 → 长度0\r\n- 查询5：5=5 → 长度1\r\n- 查询10：5<10 → 长度1\r\n```\r\n\r\n### 3. 图解说明\r\n```\r\n排序数组: [1, 2, 4, 5]\r\n前缀和:  [1, 3, 7, 12]\r\n查询值10的查找过程：\r\n        ↓ upper_bound(10)返回指向12的迭代器\r\n[1, 3, 7, 12]\r\n         ↑\r\n结果长度 = 3（迭代器位置索引）\r\n```\r\n\r\n## 三、解题思路\r\n\r\n### 1. 核心策略\r\n1. **排序优化**：将数组排序后，前k个元素的和是最小可能值\r\n2. **前缀和预处理**：计算排序后的前缀和数组\r\n3. **二分加速查询**：对每个查询值使用upper_bound快速定位\r\n\r\n### 2. 具体流程\r\n1. **预处理阶段**：\r\n   - 将原数组升序排序\r\n   - 计算前缀和数组（类型用long long防止溢出）\r\n\r\n2. **查询处理阶段**：\r\n   - 对每个查询值q：\r\n     a. 使用upper_bound查找第一个>q的前缀和位置\r\n     b. 该位置索引即为最长子序列长度\r\n   - 时间复杂度：O(n log n + m log n)\r\n\r\n## 四、代码详解\r\n\r\n```cpp\r\nclass Solution {\r\npublic:\r\n    vector<int> answerQueries(vector<int>& nums, vector<int>& queries) {\r\n        // 1. 排序原数组\r\n        sort(nums.begin(), nums.end());\r\n        \r\n        // 2. 计算前缀和数组\r\n        vector<long long> prefix;\r\n        long long sum = 0;\r\n        for (int num : nums) {\r\n            sum += num;\r\n            prefix.push_back(sum);\r\n        }\r\n        \r\n        // 3. 处理每个查询\r\n        vector<int> ans;\r\n        for (int q : queries) {\r\n            // 使用upper_bound找到第一个大于q的位置\r\n            auto it = upper_bound(prefix.begin(), prefix.end(), q);\r\n            // 迭代器距离即为可选取的元素数量\r\n            ans.push_back(it - prefix.begin());\r\n        }\r\n        \r\n        return ans;\r\n    }\r\n};\r\n```\r\n\r\n### 代码优化\r\n```cpp\r\n// 预先计算数组长度避免重复调用size()\r\nint n = nums.size();\r\nsort(nums.begin(), nums.end());\r\n\r\nvector<long long> prefix(n); // 预分配空间\r\nprefix[0] = nums[0];\r\nfor (int i = 1; i < n; ++i) {\r\n    prefix[i] = prefix[i-1] + nums[i];\r\n}\r\n```\r\n\r\n## 五、算法分析\r\n\r\n### 1. 正确性证明\r\n- **排序有效性**：子序列的和最小化需要选择最小的元素，排序后前k个元素即为最优选择\r\n- **二分正确性**：upper_bound返回第一个>q的位置，之前的元素和均≤q\r\n- **边界处理**：\r\n  - 当q < nums[0]时返回0\r\n  - 当q ≥ 总和时返回n\r\n\r\n### 2. 重要细节\r\n- **数据类型**：使用long long避免求和溢出\r\n- **STL选择**：upper_bound比lower_bound更直接满足需求\r\n- **空数组处理**：题目保证n≥1，无需额外判断\r\n\r\n## 六、复杂度分析\r\n\r\n- **时间复杂度**：O(n log n + m log n)\r\n  - 排序：O(n log n)\r\n  - m次二分查找：每次O(log n)\r\n- **空间复杂度**：O(n)\r\n  - 存储前缀和数组\r\n  - 排序使用O(log n)栈空间（快速排序）\r\n\r\n## 七、常见错误\r\n\r\n1. **错误使用lower_bound**\r\n```cpp\r\n// 错误：可能包含等于q+1的情况\r\nauto it = lower_bound(prefix.begin(), prefix.end(), q);\r\nans.push_back(it - prefix.begin());\r\n\r\n// 正确：应使用upper_bound\r\nauto it = upper_bound(prefix.begin(), prefix.end(), q);\r\n```\r\n\r\n2. **忽略整数溢出**\r\n```cpp\r\n// 错误：使用int存储前缀和\r\nvector<int> prefix;\r\nint sum = 0; // 当元素较大时会溢出\r\n```\r\n\r\n## 八、相关题目\r\n1. [209. 长度最小的子数组](https://leetcode.cn/problems/minimum-size-subarray-sum/)\r\n2. [560. 和为 K 的子数组](https://leetcode.cn/problems/subarray-sum-equals-k/)\r\n3. [1498. 满足条件的子序列数目](https://leetcode.cn/problems/number-of-subsequences-that-satisfy-the-given-sum-condition/)"}]}