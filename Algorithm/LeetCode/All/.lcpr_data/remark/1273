{"data":[{"name":"2025-02-25 09:37:01","id":1740447421424,"body":"# 1170.比较字符串最小字母出现频次\r\n\r\n## 一、题目分析\r\n\r\n### 1. 题目要求\r\n- 定义函数f(s)为字符串s中字典序最小字符的出现次数\r\n- 对于queries中的每个查询字符串q，计算words中有多少字符串w满足f(w) > f(q)\r\n- 返回一个整数数组answer，其中answer[i]是第i个查询的答案\r\n\r\n**输入输出格式**：\r\n- 输入：两个字符串数组queries和words\r\n- 输出：一个整数数组，表示每个查询的结果\r\n\r\n**约束条件**：\r\n- 1 <= queries.length, words.length <= 10^4\r\n- 1 <= queries[i].length, words[j].length <= 10\r\n- queries[i]和words[j]仅由小写英文字母组成\r\n\r\n### 2. 关键概念\r\n- **f函数**：计算字符串中字典序最小字符的出现频次\r\n- **字典序**：字母的自然排序，a < b < c < ... < z\r\n- **二分查找**：在排序数组中高效查找满足条件的元素数量\r\n\r\n## 二、示例分析\r\n\r\n### 1. 基本示例\r\n```\r\n输入：queries = [\"cbd\"], words = [\"zaaaz\"]\r\n输出：[1]\r\n解释：\r\n1. 计算f(\"cbd\") = 1，因为'b'是最小字符，出现1次\r\n2. 计算f(\"zaaaz\") = 3，因为'a'是最小字符，出现3次\r\n3. f(\"zaaaz\") > f(\"cbd\")，所以结果为1\r\n```\r\n\r\n### 2. 特殊情况\r\n```\r\n输入：queries = [\"bbb\",\"cc\"], words = [\"a\",\"aa\",\"aaa\",\"aaaa\"]\r\n输出：[1,2]\r\n解释：\r\n- f(\"bbb\") = 3，因为'b'出现3次\r\n- f(\"cc\") = 2，因为'c'出现2次\r\n- words中f值分别为：f(\"a\")=1, f(\"aa\")=2, f(\"aaa\")=3, f(\"aaaa\")=4\r\n- 对于\"bbb\"，只有\"aaaa\"的f值更大，所以结果为1\r\n- 对于\"cc\"，有\"aaa\"和\"aaaa\"的f值更大，所以结果为2\r\n```\r\n\r\n### 3. 边界情况\r\n```\r\n输入：queries = [\"a\"], words = [\"a\"]\r\n输出：[0]\r\n解释：\r\n- f(\"a\") = 1\r\n- f(\"a\") = 1\r\n- 没有word的f值严格大于query的f值，所以结果为0\r\n```\r\n\r\n### 4. 图解说明\r\n```\r\n对于查询\"cc\"和words=[\"a\",\"aa\",\"aaa\",\"aaaa\"]：\r\n\r\n1. 计算f值：\r\n   f(\"cc\") = 2\r\n   f(\"a\") = 1, f(\"aa\") = 2, f(\"aaa\") = 3, f(\"aaaa\") = 4\r\n\r\n2. 排序words的f值：[1,2,3,4]\r\n\r\n3. 二分查找：\r\n   查找第一个大于2的位置\r\n   [1,2,3,4]\r\n       ↑\r\n   找到位置2，后面有2个元素，结果为2\r\n```\r\n\r\n## 三、解题思路\r\n\r\n### 1. 核心策略\r\n- **预处理 + 二分查找**：\r\n  1. 预先计算所有words的f值并排序\r\n  2. 对每个查询，使用二分查找找到第一个大于其f值的位置\r\n  3. 计算该位置后的元素数量\r\n\r\n- **算法选择原因**：\r\n  - 直接比较每对字符串需要O(n*m)时间，效率低\r\n  - 预处理后使用二分查找将复杂度降至O(n log n)\r\n  - 排序后的数组支持高效查找大于特定值的元素数量\r\n\r\n- **可能的其他方案**：\r\n  - 计数排序：如果f值范围有限，可以使用计数数组\r\n  - 直接遍历：当数据规模小时可行，但不适合大规模数据\r\n\r\n### 2. 具体流程\r\n1. **计算f值函数**：\r\n   - 遍历字符串找到最小字符\r\n   - 统计该字符出现次数\r\n\r\n2. **预处理阶段**：\r\n   - 计算所有words的f值\r\n   - 对f值数组排序\r\n\r\n3. **查询处理阶段**：\r\n   - 对每个query计算f值\r\n   - 使用upper_bound找到第一个大于该f值的位置\r\n   - 计算该位置到数组末尾的元素数量\r\n\r\n4. **边界情况处理**：\r\n   - 当所有words的f值都不大于query的f值时，返回0\r\n   - 当所有words的f值都大于query的f值时，返回words.size()\r\n\r\n## 四、代码详解\r\n\r\n```cpp\r\nclass Solution {\r\npublic:\r\n    vector<int> numSmallerByFrequency(vector<string>& queries, vector<string>& words) {\r\n        // 预处理：计算所有words的f值并排序\r\n        vector<int> wordFreq;\r\n        for (auto& word : words) {\r\n            wordFreq.push_back(calculateF(word));\r\n        }\r\n        sort(wordFreq.begin(), wordFreq.end());\r\n        \r\n        // 处理每个查询\r\n        vector<int> res;\r\n        for (auto& query : queries) {\r\n            int fq = calculateF(query);\r\n            // 使用upper_bound找到第一个大于fq的位置\r\n            auto it = upper_bound(wordFreq.begin(), wordFreq.end(), fq);\r\n            // 计算该位置后的元素数量\r\n            res.push_back(wordFreq.end() - it);\r\n        }\r\n        return res;\r\n    }\r\n\r\nprivate:\r\n    // 计算字符串中字典序最小字符的出现次数\r\n    int calculateF(const string& s) {\r\n        char minChar = 'z'; // 初始化为最大字符\r\n        int count = 0;\r\n        \r\n        // 遍历字符串找到最小字符并计数\r\n        for (char c : s) {\r\n            if (c < minChar) {\r\n                // 找到更小的字符，重置计数\r\n                minChar = c;\r\n                count = 1;\r\n            } else if (c == minChar) {\r\n                // 相同的最小字符，增加计数\r\n                count++;\r\n            }\r\n        }\r\n        return count;\r\n    }\r\n};\r\n```\r\n\r\n### 代码优化\r\n```cpp\r\n// 优化1：预分配结果数组空间\r\nvector<int> res(queries.size());\r\nfor (int i = 0; i < queries.size(); ++i) {\r\n    int fq = calculateF(queries[i]);\r\n    res[i] = words.size() - (upper_bound(wordFreq.begin(), wordFreq.end(), fq) - wordFreq.begin());\r\n}\r\n\r\n// 优化2：使用引用避免不必要的拷贝\r\nint calculateF(const string& s) {\r\n    // 实现同上\r\n}\r\n```\r\n\r\n## 五、算法分析\r\n\r\n### 1. 正确性证明\r\n- **f函数计算正确性**：\r\n  - 遍历一次字符串，找到最小字符并计数\r\n  - 当发现更小字符时重置计数，确保只统计最小字符\r\n  \r\n- **二分查找正确性**：\r\n  - 排序后的数组满足单调性\r\n  - upper_bound返回第一个大于目标值的位置\r\n  - 该位置之后的所有元素都满足f(w) > f(q)\r\n\r\n- **特殊情况处理**：\r\n  - 当没有word的f值大于query的f值时，upper_bound返回end()，结果为0\r\n  - 当所有word的f值都大于query的f值时，upper_bound返回begin()，结果为words.size()\r\n\r\n### 2. 重要细节\r\n- **初始化minChar为'z'**：确保任何小写字母都能更新最小值\r\n- **使用upper_bound而非lower_bound**：需要严格大于而非大于等于\r\n- **计算剩余元素数量**：使用迭代器差值而非索引计算\r\n\r\n## 六、复杂度分析\r\n\r\n- **时间复杂度**: O(N + M log M + Q log M)\r\n  - 计算所有字符串的f值：O(N)，其中N是所有字符串的总长度\r\n  - 排序words的f值：O(M log M)，其中M是words数组长度\r\n  - Q次二分查找：O(Q log M)，其中Q是queries数组长度\r\n  - 总体复杂度由这三部分决定\r\n\r\n- **空间复杂度**: O(M)\r\n  - 存储words的f值数组：O(M)\r\n  - 存储结果数组：O(Q)，但这是必要的输出空间\r\n  - 排序可能需要O(log M)的额外空间\r\n\r\n## 七、常见错误\r\n\r\n1. **使用错误的二分查找函数**\r\n```cpp\r\n// 错误：使用lower_bound会包含等于的情况\r\nauto it = lower_bound(wordFreq.begin(), wordFreq.end(), fq);\r\n// 正确：应使用upper_bound找到严格大于的位置\r\nauto it = upper_bound(wordFreq.begin(), wordFreq.end(), fq);\r\n```\r\n\r\n2. **计算f值时的逻辑错误**\r\n```cpp\r\n// 错误：没有重置计数器\r\nif (c < minChar) {\r\n    minChar = c;\r\n    // 忘记重置count\r\n} \r\n// 正确：找到更小字符时重置计数\r\nif (c < minChar) {\r\n    minChar = c;\r\n    count = 1;\r\n}\r\n```\r\n\r\n## 八、相关题目\r\n- [1062. 最长重复子串](https://leetcode.cn/problems/longest-repeating-substring/)\r\n- [792. 匹配子序列的单词数](https://leetcode.cn/problems/number-of-matching-subsequences/)\r\n- [1365. 有多少小于当前数字的数字](https://leetcode.cn/problems/how-many-numbers-are-smaller-than-the-current-number/)"}]}