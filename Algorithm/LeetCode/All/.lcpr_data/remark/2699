{"data":[{"name":"2025-02-25 13:45:06","id":1740462306061,"body":"# 2563. 统计公平数对的数目\r\n\r\n## 一、题目分析\r\n\r\n### 1. 题目要求\r\n- 给定一个下标从 0 开始、长度为 n 的整数数组 nums，以及两个整数 lower 和 upper\r\n- 统计满足条件的数对 (i, j) 的数目，其中 0 ≤ i < j < n 且 lower ≤ nums[i] + nums[j] ≤ upper\r\n- 返回满足条件的数对数目\r\n\r\n### 2. 关键概念\r\n- 公平数对：两个不同位置的元素，它们的和在给定的范围[lower, upper]内\r\n- 需要注意的是数对中的两个元素必须在不同位置(i < j)\r\n- 二分查找：在排序数组中快速查找满足条件的元素范围\r\n\r\n## 二、示例分析\r\n\r\n### 1. 基本示例\r\n```\r\n输入：nums = [0,1,7,4,4,5], lower = 3, upper = 6\r\n输出：6\r\n解释：\r\n满足条件的数对有：(0,1)、(0,4)、(0,5)、(1,4)、(1,5)、(4,5)\r\n- nums[0] + nums[1] = 0 + 1 = 1 < 3，不满足条件\r\n- nums[0] + nums[4] = 0 + 4 = 4，满足 3 ≤ 4 ≤ 6\r\n- nums[0] + nums[5] = 0 + 5 = 5，满足 3 ≤ 5 ≤ 6\r\n...以此类推\r\n```\r\n\r\n### 2. 特殊情况\r\n```\r\n输入：nums = [1,7,9,2,5], lower = 11, upper = 11\r\n输出：1\r\n解释：\r\n只有一个数对满足条件：(1,2)，因为 nums[1] + nums[2] = 7 + 9 = 16 > 11，不满足条件\r\nnums[0] + nums[3] = 1 + 2 = 3 < 11，不满足条件\r\nnums[3] + nums[4] = 2 + 5 = 7 < 11，不满足条件\r\n只有 nums[0] + nums[4] = 1 + 5 = 6 < 11，不满足条件\r\n只有 nums[1] + nums[3] = 7 + 2 = 9 < 11，不满足条件\r\n只有 nums[2] + nums[4] = 9 + 5 = 14 > 11，不满足条件\r\n```\r\n\r\n## 三、解题思路\r\n\r\n### 1. 核心策略\r\n- 首先对数组进行排序，这样可以使用二分查找\r\n- 对于每个元素nums[i]，我们需要找到所有满足lower ≤ nums[i] + nums[j] ≤ upper且j > i的元素j\r\n- 使用二分查找来高效地找到这些元素\r\n\r\n### 2. 具体流程\r\n1. 对数组nums进行排序\r\n2. 初始化计数器sum为0\r\n3. 遍历数组中的每个元素nums[i]：\r\n   - 对于nums[i+1:]部分，找到第一个使得nums[i] + nums[j] ≥ lower的位置left\r\n   - 对于nums[i+1:]部分，找到第一个使得nums[i] + nums[j] > upper的位置right\r\n   - 将right - left加到sum上\r\n4. 返回sum作为结果\r\n\r\n## 四、代码详解\r\n\r\n```cpp\r\nclass Solution {\r\npublic:\r\n    long long countFairPairs(vector<int>& nums, int lower, int upper) {\r\n        int n = nums.size();\r\n        // 首先对数组排序，以便使用二分查找\r\n        sort(nums.begin(), nums.end());\r\n        long long sum = 0;\r\n        \r\n        for (int i = 0; i < n; i++) {\r\n            // 对于每个nums[i]，找到满足条件的nums[j]范围\r\n            // 需要找到j > i且lower ≤ nums[i] + nums[j] ≤ upper\r\n            \r\n            // 找到第一个使得nums[i] + nums[j] >= lower的位置\r\n            auto it_lower = lower_bound(nums.begin() + i + 1, nums.end(), lower - nums[i]);\r\n            \r\n            // 找到第一个使得nums[i] + nums[j] > upper的位置\r\n            auto it_upper = upper_bound(nums.begin() + i + 1, nums.end(), upper - nums[i]);\r\n            \r\n            // 计算满足条件的元素数量并累加\r\n            sum += (it_upper - it_lower);\r\n        }\r\n        \r\n        return sum;\r\n    }\r\n};\r\n```\r\n\r\n### 代码优化\r\n```cpp\r\n// 使用ranges库可以简化代码，但需要确保编译器支持C++20\r\nclass Solution {\r\npublic:\r\n    long long countFairPairs(vector<int>& nums, int lower, int upper) {\r\n        sort(nums.begin(), nums.end());\r\n        long long sum = 0;\r\n        \r\n        for (int i = 0; i < nums.size(); i++) {\r\n            auto it_lower = lower_bound(nums.begin() + i + 1, nums.end(), lower - nums[i]);\r\n            auto it_upper = upper_bound(nums.begin() + i + 1, nums.end(), upper - nums[i]);\r\n            sum += (it_upper - it_lower);\r\n        }\r\n        \r\n        return sum;\r\n    }\r\n};\r\n```\r\n\r\n## 五、算法分析\r\n\r\n### 1. 正确性证明\r\n- 排序后，对于每个nums[i]，我们需要找到所有j > i且lower ≤ nums[i] + nums[j] ≤ upper的元素j\r\n- 这等价于找到所有j > i且lower - nums[i] ≤ nums[j] ≤ upper - nums[i]的元素j\r\n- 在排序数组中，我们可以使用二分查找高效地找到这个范围\r\n- 通过累加每个元素的贡献，我们可以得到总的公平数对数量\r\n\r\n### 2. 重要细节\r\n- 必须确保j > i，所以我们只在nums[i+1:]部分中查找\r\n- 使用lower_bound和upper_bound函数来进行二分查找\r\n- 注意计算差值时使用的是迭代器差值，这直接给出了元素数量\r\n\r\n## 六、复杂度分析\r\n\r\n- **时间复杂度**: O(n log n)\r\n  - 排序需要O(n log n)时间\r\n  - 对每个元素进行二分查找需要O(log n)时间，总共有n个元素，所以这部分是O(n log n)\r\n  - 总体时间复杂度为O(n log n)\r\n  \r\n- **空间复杂度**: O(log n)或O(n)\r\n  - 排序可能需要O(log n)的栈空间\r\n  - 如果使用非原地排序算法，可能需要O(n)的额外空间\r\n\r\n## 七、常见错误\r\n\r\n1. **忽略位置限制**\r\n```cpp\r\n// 错误代码示例\r\nfor (int i = 0; i < n; i++) {\r\n    auto it1 = lower_bound(nums.begin(), nums.end(), lower - nums[i]);\r\n    auto it2 = upper_bound(nums.begin(), nums.end(), upper - nums[i]);\r\n    sum += it2 - it1;\r\n}\r\n```\r\n这段代码错误地在整个数组中查找，没有考虑j > i的限制，可能会将(i,i)这样的无效数对也计算在内。\r\n\r\n2. **边界条件处理不当**\r\n```cpp\r\n// 错误代码示例\r\nfor (int i = 0; i < n - 1; i++) {\r\n    auto it1 = lower_bound(nums.begin() + i + 1, nums.end(), lower - nums[i]);\r\n    auto it2 = upper_bound(nums.begin() + i + 1, nums.end(), upper - nums[i]);\r\n    if (it1 != nums.end() && it2 != nums.end())\r\n        sum += it2 - it1;\r\n}\r\n```\r\n这段代码不必要地检查迭代器是否到达end()，因为lower_bound和upper_bound返回的迭代器总是有效的，即使没有找到满足条件的元素。\r\n\r\n## 八、相关题目\r\n- [1. 两数之和](https://leetcode.cn/problems/two-sum/)\r\n- [167. 两数之和 II - 输入有序数组](https://leetcode.cn/problems/two-sum-ii-input-array-is-sorted/)\r\n- [15. 三数之和](https://leetcode.cn/problems/3sum/)\r\n\r\n---\r\n\r\n通过这个详细的解析，我们可以看到这道题目的核心是利用排序和二分查找来高效地找到满足条件的数对。关键在于理解如何将原问题转化为在排序数组中查找特定范围内的元素，并正确处理索引限制。"}]}