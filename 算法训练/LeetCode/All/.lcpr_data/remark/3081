{"data":[{"name":"2025-02-25 14:08:41","id":1740463721015,"body":"# 2856. 删除数对后的最小数组长度\r\n\r\n## 一、题目分析\r\n\r\n### 1. 题目要求\r\n- 给定一个非递减顺序排列的整数数组 `nums`\r\n- 你可以执行任意次下述操作：从 `nums` 中选择两个不同的值 `x` 和 `y`，并将它们从数组中删除\r\n- 返回执行上述操作后数组可能的最小长度\r\n\r\n### 2. 关键概念\r\n- 非递减数组：数组中的元素按照从小到大排序，允许有重复元素\r\n- 删除操作：每次必须删除两个不同的值（不是索引）\r\n- 最小长度：通过合理选择删除的数对，使最终数组长度最小\r\n\r\n## 二、示例分析\r\n\r\n### 1. 基本示例\r\n```\r\n输入：[1,2,3,4]\r\n输出：0\r\n解释：\r\n可以按照以下步骤将数组长度降为 0：\r\n- 选择值 1 和 2，将它们从数组中删除，得到 [3,4]\r\n- 选择值 3 和 4，将它们从数组中删除，得到 []\r\n最终数组的长度为 0\r\n```\r\n\r\n### 2. 有重复元素的情况\r\n```\r\n输入：[1,1,2,2,3,3]\r\n输出：0\r\n解释：\r\n可以按照以下步骤将数组长度降为 0：\r\n- 选择值 1 和 2，将它们从数组中删除，得到 [1,2,3,3]\r\n- 选择值 1 和 2，将它们从数组中删除，得到 [3,3]\r\n- 选择值 3 和 3，将它们从数组中删除，得到 []\r\n注意，虽然值相同，但在第三步中选择的是两个不同的元素（索引不同）\r\n```\r\n\r\n### 3. 特殊情况\r\n```\r\n输入：[1000000000,1000000000]\r\n输出：0\r\n解释：\r\n可以选择两个值 1000000000 删除，因为它们是不同的元素（索引不同）\r\n```\r\n\r\n### 4. 频率不均的情况\r\n```\r\n输入：[2,3,4,4,4]\r\n输出：1\r\n解释：\r\n可以按照以下步骤将数组长度降为 1：\r\n- 选择值 2 和 3，将它们从数组中删除，得到 [4,4,4]\r\n- 选择值 4 和 4，将它们从数组中删除，得到 [4]\r\n无法进一步删除，因为只剩下一个元素，最终数组的长度为 1\r\n```\r\n\r\n## 三、解题思路\r\n\r\n### 1. 核心策略\r\n- 关键观察：如果数组中某个元素的出现次数超过数组长度的一半，那么这个元素会限制我们能删除的数对数量\r\n- 我们应该尽可能地将不同的元素配对删除\r\n- 最优策略是找出出现次数最多的元素，然后计算它与其他元素能组成多少对\r\n\r\n### 2. 具体流程\r\n1. 找出数组中出现次数最多的元素及其出现次数 `max_cnt`\r\n2. 计算其他元素的总数 `other_cnt = n - max_cnt`\r\n3. 如果 `max_cnt <= other_cnt`，那么所有元素都可以配对删除，最终长度为 `n % 2`（如果n为奇数，会剩下一个元素）\r\n4. 如果 `max_cnt > other_cnt`，那么最多能删除 `other_cnt` 对，剩余长度为 `max_cnt - other_cnt`\r\n\r\n## 四、代码详解\r\n\r\n```cpp\r\nclass Solution {\r\npublic:\r\n    int minLengthAfterRemovals(vector<int>& nums) {\r\n        int n = nums.size();\r\n        \r\n        // 找出出现次数最多的元素\r\n        int max_cnt = 0;\r\n        int count = 1;\r\n        for (int i = 1; i < n; ++i) {\r\n            if (nums[i] == nums[i-1]) {\r\n                count++;\r\n            } else {\r\n                max_cnt = max(max_cnt, count);\r\n                count = 1;\r\n            }\r\n        }\r\n        max_cnt = max(max_cnt, count); // 不要忘记最后一组元素\r\n        \r\n        // 计算其他元素的总数\r\n        int other_cnt = n - max_cnt;\r\n        \r\n        // 如果最大频率元素不超过一半，可以全部配对（可能剩余一个）\r\n        if (max_cnt <= other_cnt) {\r\n            return n % 2;\r\n        }\r\n        // 否则，剩余的长度为 max_cnt - other_cnt\r\n        else {\r\n            return max_cnt - other_cnt;\r\n        }\r\n    }\r\n};\r\n```\r\n\r\n### 代码优化\r\n```cpp\r\nclass Solution {\r\npublic:\r\n    int minLengthAfterRemovals(vector<int>& nums) {\r\n        int n = nums.size();\r\n        \r\n        // 由于数组已排序，中间元素的值可以帮助我们找到最大频率\r\n        int x = nums[n / 2]; // 取中间元素\r\n        \r\n        // 使用二分查找找到x的范围\r\n        auto left = lower_bound(nums.begin(), nums.end(), x);\r\n        auto right = upper_bound(nums.begin(), nums.end(), x);\r\n        int max_cnt = right - left; // x的出现次数\r\n        \r\n        // 计算其他元素的总数\r\n        int other_cnt = n - max_cnt;\r\n        \r\n        // 如果最大频率元素不超过一半，可以全部配对（可能剩余一个）\r\n        if (max_cnt <= other_cnt) {\r\n            return n % 2;\r\n        }\r\n        // 否则，剩余的长度为 max_cnt - other_cnt\r\n        else {\r\n            return max_cnt - other_cnt;\r\n        }\r\n    }\r\n};\r\n```\r\n\r\n## 五、算法分析\r\n\r\n### 1. 正确性证明\r\n- 如果数组中没有元素出现次数超过一半，那么我们总是可以将不同的元素配对删除，最多剩下一个元素（当n为奇数时）\r\n- 如果某个元素出现次数超过一半，那么这个元素会限制我们能删除的数对数量\r\n- 最多能删除的数对数量等于其他元素的总数，因为每个其他元素都可以与一个最大频率元素配对\r\n- 因此，剩余的长度为 `max_cnt - other_cnt`\r\n\r\n### 2. 重要细节\r\n- 数组已经按非递减顺序排列，这使得我们可以更容易地计算元素的频率\r\n- 中间元素是一个很好的候选，因为如果有元素出现次数超过一半，它一定会是中间元素\r\n- 使用二分查找可以高效地找到元素的出现范围\r\n\r\n## 六、复杂度分析\r\n\r\n- **时间复杂度**: O(n)\r\n  - 遍历数组计算最大频率需要O(n)时间\r\n  - 使用二分查找优化后，找到元素范围需要O(log n)时间\r\n  - 总体时间复杂度为O(n)\r\n  \r\n- **空间复杂度**: O(1)\r\n  - 只使用了常数额外空间\r\n\r\n## 七、常见错误\r\n\r\n1. **忽略最后一组元素**\r\n```cpp\r\n// 错误代码示例\r\nint max_cnt = 0;\r\nint count = 1;\r\nfor (int i = 1; i < n; ++i) {\r\n    if (nums[i] == nums[i-1]) {\r\n        count++;\r\n    } else {\r\n        max_cnt = max(max_cnt, count);\r\n        count = 1;\r\n    }\r\n}\r\n// 没有处理最后一组元素\r\n```\r\n\r\n2. **错误理解题目要求**\r\n```cpp\r\n// 错误代码示例 - 假设只能删除相同的元素\r\nint minLengthAfterRemovals(vector<int>& nums) {\r\n    unordered_map<int, int> freq;\r\n    for (int num : nums) {\r\n        freq[num]++;\r\n    }\r\n    int pairs = 0;\r\n    for (auto& [_, count] : freq) {\r\n        pairs += count / 2;\r\n    }\r\n    return nums.size() - pairs * 2;\r\n}\r\n```\r\n\r\n## 八、相关题目\r\n- [1338. 数组大小减半](https://leetcode.cn/problems/reduce-array-size-to-the-half/)\r\n- [1497. 检查数组对是否可以被 k 整除](https://leetcode.cn/problems/check-if-array-pairs-are-divisible-by-k/)\r\n- [2007. 从双倍数组中还原原数组](https://leetcode.cn/problems/find-original-array-from-doubled-array/)\r\n\r\n---\r\n\r\n这道题的关键在于理解：当我们可以删除任意两个不同的值时，限制因素是出现频率最高的元素。如果某个元素出现次数超过数组长度的一半，那么它会限制我们能删除的数对数量。通过计算最大频率和其他元素的数量关系，我们可以得到最小可能的数组长度。"}]}