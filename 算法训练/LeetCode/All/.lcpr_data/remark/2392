{"data":[{"name":"2025-02-24 15:21:53","id":1740381713294,"body":"# 2300.咒语和药水的成功对数\r\n\r\n## 一、题目分析\r\n\r\n### 1. 题目要求\r\n给定两个正整数数组spells（咒语）和potions（药水），以及一个成功阈值success。要求对每个咒语，统计能与多少药水组成成功组合（乘积≥success），返回结果数组。\r\n\r\n约束条件：\r\n- 1 <= spells.length, potions.length <= 1e5\r\n- 1 <= spells[i], potions[j] <= 1e5\r\n- 1 <= success <= 1e10\r\n\r\n### 2. 关键概念\r\n- 乘积计算：spell * potion ≥ success\r\n- 高效查找：由于数据规模大，需避免O(n*m)暴力解法\r\n- 排序与二分：预处理后利用有序性加速查找\r\n\r\n## 二、示例分析\r\n\r\n### 1. 基本示例\r\n输入：spells = [5,1,3], potions = [1,2,3,4,5], success = 7  \r\n输出：[4,0,3]  \r\n解释：\r\n- 咒语5需要药水≥2（7/5=1.4→向上取整2），在排序后的药水中找到2的位置，后续4个元素满足\r\n- 咒语1需要药水≥7，但最大药水5不满足\r\n- 咒语3需要药水≥3（7/3≈2.33→3），找到3的位置后剩余3个元素\r\n\r\n### 2. 边界情况\r\n输入：spells = [1], potions = [1], success = 1  \r\n输出：[1]  \r\n解释：1*1=1刚好满足条件\r\n\r\n### 3. 图解说明\r\n```\r\n排序后药水数组：[1,2,3,4,5]\r\n咒语=3时，需求阈值=3\r\n          ↓ 第一个≥3的位置\r\n[1,2,3,4,5]\r\n    ↑\r\n剩余3个元素（3,4,5）\r\n```\r\n\r\n## 三、解题思路\r\n\r\n### 1. 核心策略\r\n1. 预处理：排序药水数组（O(m log m)）\r\n2. 对每个咒语spell：\r\n   - 计算最小需求值target = ceil(success / spell)\r\n   - 二分查找第一个≥target的药水位置\r\n   - 统计右侧所有药水数量\r\n\r\n### 2. 具体流程\r\n1. 排序药水数组（升序）\r\n2. 遍历每个咒语：\r\n   a. 计算目标值target = (success + spell - 1) / spell（避免浮点运算）\r\n   b. 使用lower_bound在药水数组中查找target\r\n   c. 结果 = 数组长度 - 找到的位置索引\r\n3. 返回结果数组\r\n\r\n## 四、代码详解\r\n\r\n```cpp\r\nclass Solution {\r\npublic:\r\n    vector<int> successfulPairs(vector<int>& spells, vector<int>& potions, long long success) {\r\n        sort(potions.begin(), potions.end());  // 预处理排序O(m log m)\r\n        vector<int> ans;\r\n        \r\n        for (int spell : spells) {\r\n            // 计算需要的最小药水强度（向上取整）\r\n            long long target = (success + spell - 1) / spell;\r\n            \r\n            // 二分查找第一个≥target的位置\r\n            auto it = lower_bound(potions.begin(), potions.end(), target);\r\n            \r\n            // 统计满足条件的数量\r\n            ans.push_back(potions.end() - it);\r\n        }\r\n        return ans;\r\n    }\r\n};\r\n```\r\n\r\n### 代码优化\r\n```cpp\r\n// 预先计算药水数组长度避免重复计算\r\nint m = potions.size();\r\n...\r\nans.push_back(m - (it - potions.begin()));\r\n```\r\n\r\n## 五、算法分析\r\n\r\n### 1. 正确性证明\r\n- 排序不影响结果：乘积大小关系只与数值本身有关，与顺序无关\r\n- 二分查找正确性：lower_bound返回第一个不小于target的值，确保后续元素均≥target\r\n- 边界处理：当target超过最大值时返回0，当target≤最小值时返回全部\r\n\r\n### 2. 重要细节\r\n- 使用long long防止乘法溢出\r\n- 向上取整技巧：(a + b - 1) / b 等价于ceil(a/b)\r\n- lower_bound的STL使用：在有序数组中快速定位\r\n\r\n## 六、复杂度分析\r\n\r\n- **时间复杂度**：O(m log m + n log m)  \r\n  - 排序药水数组：O(m log m)  \r\n  - n次二分查找：每次O(log m) → 总O(n log m)\r\n- **空间复杂度**：O(log m) ~ O(m)  \r\n  - 排序的栈空间（快速排序最差O(m)）  \r\n  - 结果数组空间O(n)不计入额外空间\r\n\r\n## 七、常见错误\r\n\r\n1. **整数溢出**\r\n```cpp\r\n// 错误：直接使用int相乘\r\nint product = spell * potion;  // 可能溢出\r\n// 正确：转换为long long\r\nlong long product = (long long)spell * potion;\r\n```\r\n\r\n2. **错误的上取整计算**\r\n```cpp\r\n// 错误：直接整数除法\r\nint target = success / spell;  // 向下取整\r\n// 正确：使用数学技巧\r\nlong long target = (success + spell - 1) / spell;\r\n```\r\n\r\n## 八、相关题目\r\n1. [两数之和 II - 输入有序数组](https://leetcode.cn/problems/two-sum-ii-input-array-is-sorted/)\r\n2. [乘积小于 K 的子数组](https://leetcode.cn/problems/subarray-product-less-than-k/)\r\n3. [找到 K 个最接近的元素](https://leetcode.cn/problems/find-k-closest-elements/)"},{"name":"2025-02-25 10:53:20","id":1740452000329,"body":"# 2300.咒语和药水的成功对数\r\n\r\n## 问题理解\r\n\r\n这道题目给我们两个数组：\r\n- `spells`：表示咒语的强度\r\n- `potions`：表示药水的强度\r\n\r\n当一个咒语和一个药水的强度乘积 ≥ `success` 时，这个组合被视为成功。我们需要计算每个咒语能与多少个药水组成成功组合。\r\n\r\n## 算法思路\r\n\r\n### 1. 问题转化\r\n\r\n对于每个咒语 `spell`，我们需要找出满足以下条件的药水数量：\r\n```\r\nspell * potion >= success\r\n```\r\n\r\n这个不等式可以转化为：\r\n```\r\npotion >= success / spell\r\n```\r\n\r\n因此，对于每个咒语，我们需要找出有多少个药水的强度大于等于 `success / spell`。\r\n\r\n### 2. 使用二分查找优化\r\n\r\n如果我们对药水数组进行排序，就可以使用二分查找来快速找到第一个满足条件的药水，然后计算出满足条件的药水总数。\r\n\r\n### 3. 算法步骤\r\n\r\n1. 对药水数组 `potions` 进行排序\r\n2. 对于每个咒语 `spell`：\r\n   - 计算满足条件的最小药水强度：`minPotion = ceil(success / spell)`\r\n   - 处理边界情况：\r\n     - 如果 `minPotion > potions[m-1]`，则没有成功组合\r\n     - 如果 `minPotion <= potions[0]`，则所有药水都能组成成功组合\r\n   - 使用二分查找找到第一个大于等于 `minPotion` 的药水位置\r\n   - 计算满足条件的药水数量：`m - position`\r\n\r\n## 代码详解\r\n\r\n```cpp\r\nvector<int> successfulPairs(vector<int>& spells, vector<int>& potions, long long success) {\r\n    int n = spells.size();\r\n    int m = potions.size();\r\n    vector<int> pairs(n, 0);\r\n    \r\n    // 先对药水数组排序，便于二分查找\r\n    sort(potions.begin(), potions.end());\r\n    \r\n    for (int i = 0; i < n; i++) {\r\n        // 计算当前咒语需要的最小药水强度\r\n        // 使用 ceil(success / spell) 来找到满足条件的最小药水强度\r\n        long long minPotion = (success + spells[i] - 1) / spells[i];\r\n        \r\n        // 边界情况1：如果最小所需强度大于最大药水强度，则没有成功组合\r\n        if (minPotion > potions[m - 1]) {\r\n            pairs[i] = 0;\r\n            continue;\r\n        }\r\n        \r\n        // 边界情况2：如果最小所需强度小于等于最小药水强度，则所有药水都能组成成功组合\r\n        if (minPotion <= potions[0]) {\r\n            pairs[i] = m;\r\n            continue;\r\n        }\r\n        \r\n        // 二分查找第一个大于等于 minPotion 的药水\r\n        int left = 0, right = m - 1;\r\n        while (left < right) {\r\n            int mid = left + (right - left) / 2;\r\n            if (potions[mid] < minPotion) {\r\n                left = mid + 1;\r\n            } else {\r\n                right = mid;\r\n            }\r\n        }\r\n        \r\n        // 计算满足条件的药水数量\r\n        pairs[i] = m - left;\r\n    }\r\n    \r\n    return pairs;\r\n}\r\n```\r\n\r\n### 关键点解释\r\n\r\n1. **整数除法的向上取整**：\r\n   ```cpp\r\n   long long minPotion = (success + spells[i] - 1) / spells[i];\r\n   ```\r\n   这是一个常用技巧，等价于 `ceil(success / spells[i])`。例如，如果 `success = 10` 且 `spells[i] = 3`，则 `(10 + 3 - 1) / 3 = 12 / 3 = 4`，这正是 `ceil(10/3)`。\r\n\r\n2. **二分查找的实现**：\r\n   ```cpp\r\n   int left = 0, right = m - 1;\r\n   while (left < right) {\r\n       int mid = left + (right - left) / 2;\r\n       if (potions[mid] < minPotion) {\r\n           left = mid + 1;\r\n       } else {\r\n           right = mid;\r\n       }\r\n   }\r\n   ```\r\n   这是一个寻找第一个大于等于目标值的二分查找变种。当循环结束时，`left` 指向第一个大于等于 `minPotion` 的位置。\r\n\r\n3. **边界情况处理**：\r\n   - 如果所有药水都太弱（`minPotion > potions[m-1]`），则没有成功组合\r\n   - 如果所有药水都足够强（`minPotion <= potions[0]`），则所有药水都能组成成功组合\r\n\r\n## 复杂度分析\r\n\r\n### 时间复杂度\r\n- 排序药水数组：O(m log m)\r\n- 对每个咒语进行二分查找：O(n log m)\r\n- 总时间复杂度：O(m log m + n log m)\r\n\r\n### 空间复杂度\r\n- 结果数组：O(n)\r\n- 排序可能需要额外空间（取决于排序算法实现）：O(log m)\r\n- 总空间复杂度：O(n)\r\n\r\n## 示例演示\r\n\r\n以示例1为例：`spells = [5,1,3]`, `potions = [1,2,3,4,5]`, `success = 7`\r\n\r\n1. 排序 `potions`（已经排序）：`[1,2,3,4,5]`\r\n\r\n2. 对于 `spell = 5`：\r\n   - `minPotion = ceil(7/5) = 2`\r\n   - 二分查找找到位置 `1`（值为2）\r\n   - 满足条件的药水数量：`5 - 1 = 4`\r\n\r\n3. 对于 `spell = 1`：\r\n   - `minPotion = ceil(7/1) = 7`\r\n   - 所有药水都小于7，所以没有成功组合\r\n   - 满足条件的药水数量：`0`\r\n\r\n4. 对于 `spell = 3`：\r\n   - `minPotion = ceil(7/3) = 3`\r\n   - 二分查找找到位置 `2`（值为3）\r\n   - 满足条件的药水数量：`5 - 2 = 3`\r\n\r\n最终结果：`[4,0,3]`\r\n\r\n这与预期输出一致，验证了我们的算法是正确的。"}]}